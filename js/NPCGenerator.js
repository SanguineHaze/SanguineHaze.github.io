angular.module('sangApp')//get your app, then add stuff to it this will seem a little odd at first.

//factory loads in all the things you'll need this is your constructor basically.
.factory('NPCGeneratorFactory', function () {

})
//controller does the fancy stuff and has any methods your front end uses.
.controller('NPCGeneratorController', function ($scope, NPCGeneratorFactory) {
    
})
;